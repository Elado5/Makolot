//? Makes it easier to read in 'api.js' file and prevents rewriting of the long address.
export const baseAPI = "http://localhost:5000/api";

//? later can be changed to the actual address and will change everywhere easily.